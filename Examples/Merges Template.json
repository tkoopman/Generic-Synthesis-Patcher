/*
  Perform merges of some stuff, ready to add excludes if required
*/

[
  {
    "types": "container",
    "singleMatch": true,
    "rules": [
      // Add sections here to exclude from merge
      // { "!formID": [] },
      // { "!masters": [] },

      // For everything else just do basic merge
      { "merge": { "Items": [ ] } }
    ]
  },
  {
    "types": [ "ammunition", "armor", "container", "ingestible", "ingredient" ],
    "singleMatch": true,
    "rules": [
      // Add sections here to exclude from merge
      // { "!formID": [] },
      // { "!masters": [] },

      // For everything else just do basic merge
      {
        "merge": { "Keywords": [ "-Immersive Sounds - Compendium.esp" ] }
      }
    ]
  },
  {
    "types": "formList",
    "singleMatch": true,
    "rules": [
      // Add sections here to exclude from merge
      // { "!formID": [] },
      // { "!masters": [] },

      // For everything else just do basic merge
      { "merge": { "Items": [ ] } }
    ]
  },
  {
    "types": [ "ingestible", "ingredient" ],
    "singleMatch": true,
    "rules": [
      // Add sections here to exclude from merge
      // { "!formID": [] },
      // { "!masters": [] },

      // For everything else just do basic merge
      { "merge": { "Effects": [ ] } }
    ]
  },
  {
    "types": "leveledItem",
    "singleMatch": true,
    "rules": [
      // Add sections here to exclude from merge
      // { "!formID": [] },
      // { "!masters": [] },

      // For everything else just do basic merge
      { "merge": { "Entries": [ ] } }
    ]
  },
  {
    "types": "leveledNpc",
    "singleMatch": true,
    "rules": [
      // Add sections here to exclude from merge
      // { "!formID": [] },
      // { "!masters": [] },

      // For everything else just do basic merge
      { "merge": { "Entries": [ ] } }
    ]
  },
  {
    "types": "leveledSpell",
    "singleMatch": true,
    "rules": [
      // Add sections here to exclude from merge
      // { "!formID": [] },
      // { "!masters": [] },

      // For everything else just do basic merge
      { "merge": { "Entries": [ ] } }
    ]
  },
  {
    "types": "faction",
    "singleMatch": true,
    "rules": [
      // Add sections here to exclude from merge
      // { "!formID": [] },
      // { "!masters": [] },

      // For everything else just do basic merge
      { "merge": { "Relations": [ ] } }
    ]
  }
]